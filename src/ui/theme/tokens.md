# Theme Tokens (Scaffolding)

TODO: Define canonical design tokens (spacing, radii, shadows, motion) once the UI contract is ready.

This file exists to document future UI decisions without changing the current theme.
